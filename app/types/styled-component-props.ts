/**
 * This interface defines default props so other components do not need to define them themselves
 */
interface IDefaultProps {
  /** needs class name prop for styled-components */
  className?: string;
}

export default IDefaultProps;
